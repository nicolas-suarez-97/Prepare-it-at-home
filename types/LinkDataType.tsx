export type LinkDataType = {
    buttonStyle?: string,
    course: string,
    discount?: number,
    isModalOpen?: boolean,
    label?: string,
    onClickAction?: Function,
    url: string,
};